import React from 'react'
import { Button } from 'react-bootstrap'

export const ButtonViewRecibo = ({recibo}) => {
  return (
    <Button variant="outline-secondary">
        Descargar
    </Button>
  )
}
